const results = {
    init: function() {
        let urlParams = utils.getUrlVars();
        
    },
    showFinal: function() {
        console.log('final results revealed');
    }
};

export default results;